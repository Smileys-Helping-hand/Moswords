# ⚠️  This file is listed in .gitignore — never commit it.
# Copy this template, fill in your values, and save as  android/keystore.properties
#
# Create the keystore first:
#   npm run keystore:create
#   (answer the prompts — remember the passwords you set)
#
storeFile=../keystore/moswords-release.keystore
storePassword=CHANGE_ME
keyAlias=moswords
keyPassword=CHANGE_ME
